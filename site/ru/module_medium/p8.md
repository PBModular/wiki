# 📋 Получение списка загруженных модулей

В каждом модуле есть property-метод для получения списка объектов `ModuleInfo` (информация о модуле, прописанная в `config.yaml`). Пример использования:

```python
...
mods = self.loaded_modules
for mod in mods:
...
```
